# GPU(CUDA 11.8) 사용 시: 아래 인덱스에서 설치해야 한다.
#   pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu118
# cu118은 NVIDIA 드라이버 452.39 이상이면 동작한다(개발 PC는 457.20).
torch==2.7.1
transformers>=4.40
scikit-learn>=1.3
numpy>=1.24
